CREATE TABLE IF NOT EXISTS country (
                       id int(11) NOT NULL AUTO_INCREMENT,
                       name VARCHAR(40),
                       peopleCount BIGINT(20),
                       PRIMARY KEY (id)
)